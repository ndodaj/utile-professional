spring:
  application:
    name: professional-service
#  cloud:
#    inetutils:
#      ignored-interfaces:
#        - ".*"

  datasource:
    url: jdbc:mysql://localhost:3306/utile
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: true
    generate-ddl: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

logging:
  level:
    root: DEBUG

server:
  port: 8081

#management:
#  endpoints:
#    web:
#      exposure:
#        include: "*"
#
#eureka:
#  client:
#    service-url:
#      defaultZone: http://localhost:8761/eureka/
#    register-with-eureka: true
#    fetch-registry: true